[package]
name = "ui"
version = "0.1.0"
edition = "2024"

[dependencies]
reactive_core = { path = "../core", package = "core" }

[target.'cfg(target_os = "macos")'.dependencies]
appkit = { path = "../appkit" }
